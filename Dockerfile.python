# Используем официальный Python-образ (slim-версия для минимального размера)
FROM python:3.11-slim

# Устанавливаем рабочую директорию
WORKDIR /app

# Устанавливаем системные зависимости и NordVPN
RUN apt-get update && apt-get install -y --no-install-recommends \
    ffmpeg curl gnupg2 iproute2 openvpn iptables sudo && \
    curl -sSf https://downloads.nordcdn.com/apps/linux/install.sh | sh -s -- --nordvpn && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

# Устанавливаем yt-dlp для скачивания видео
RUN curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp && \
    chmod a+rx /usr/local/bin/yt-dlp

# Копируем файлы проекта в контейнер
COPY requirements.txt /app/requirements.txt
COPY . /app

# Устанавливаем зависимости внутри виртуального окружения
RUN python -m venv venv && \
    ./venv/bin/pip install --upgrade pip && \
    ./venv/bin/pip install -r requirements.txt

# Устанавливаем переменные окружения
ENV API_TOKEN=""
ENV LOCAL_API_URL=""
ENV MONGO_URI=""
ENV NORDVPN_TOKEN=""
ENV PYTHONPATH=/app

# Настройка NordVPN перед запуском
CMD sh -c "\
    echo 'Запуск NordVPN...' && \
    nohup nordvpnd > /dev/null 2>&1 & \
    sleep 5 && \
    nordvpn set technology nordlynx && \
    nordvpn set killswitch on && \
    nordvpn login --token $NORDVPN_TOKEN && \
    nordvpn connect && \
    echo 'Проверка IP:' && \
    curl -s https://ifconfig.me && echo '' && \
    echo 'NordVPN подключен. Запуск бота...' && \
    ./venv/bin/python bot/main.py"
